<!--하위 컴포넌트-->

<template>
  <slot name="header"></slot>
  <h3>PageTitle</h3>
  <p>{{ title }}</p>
  <p>{{ likes }}</p>
  <p v-bind:class="{ 'red-font': isOK }">환영합니다</p>
  <!--isOK는 true, false 반환.-->
  <slot></slot>
  <ul>
    <li v-bind:key="i" v-for="(member, i) in members">
      이름: {{ member.name }}, 나이: {{ member.age }}
    </li>
  </ul>
  <slot name="footer"></slot>
</template>

<script>
export default {
  data() {
    return {
      msg: "",
    };
  },
  // 상위요소에서 하위요소로 정보를 전달할 때 : props 속성 사용.
  props: {
    title: { type: String, default: "페이지 제목" }, // 값이 넘어오지 않으면 default값으로 사용
    likes: { type: Number, default: 0 },
    isOK: { type: Boolean, default: false },
    members: {
      type: Array,
      default: function () {
        return [{ name: "삼촌", age: 33 }];
      },
    },
  },
};
</script>

<style scoped>
.red-font {
  color: rgb(228, 20, 106);
}
</style>
